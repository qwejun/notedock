// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * What the settings panel shows under "关于". The data directory is worth
 * surfacing: it is where the offline cache and the bearer token live.
 */
export type AppInfo = { version: string, data_dir: string, db_path: string, };

/**
 * A note as the floating window sees it: metadata only, plus `dirty` so the sync
 * dot can be honest about a creation that has not been uploaded.
 */
export type LocalNote = { id: string, title: string, preview: string, rev: number, updated_at: string, dirty: boolean, };

export type SyncState = { status: SyncStatus, 
/**
 * Human-readable detail, shown only when there is something to say.
 */
message: string | null, 
/**
 * Local creations and deletions not yet accepted by the server.
 */
pending: number, logged_in: boolean, server_url: string, };

export type SyncStatus = "synced" | "syncing" | "offline";

/**
 * Window preferences that outlive the session.
 */
export type WindowPrefs = { opacity: number, always_on_top: boolean, };
